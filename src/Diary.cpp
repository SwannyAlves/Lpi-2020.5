#include "Diary.h"

//para inicializar variaveis da struct coloca ":nome_da_variavel(inicializa)"
Diary::Diary(const std::string& filename) : messages(), filename()
{
}

void Diary::add(const std::string& message) 
{
    // adicionar mensagem no array de mensagens
}

void Diary::write()
{
    // gravar as mensagens no disco
}
